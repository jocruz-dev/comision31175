
const Function = ({items}) => {

    return(
        <div>
            <h1>Componente de funcion</h1>
            {items.map((value, index)=> <p>HOLA</p> )}
        </div>
    )
}

export default Function

